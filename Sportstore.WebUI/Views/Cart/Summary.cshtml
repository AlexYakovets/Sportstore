@model Sportore.Domain.Entities.Cart

<div id="cart">
    <span class="caction">
    <b>Your cart</b>
    @Model.Lines.Sum(x=>x.Quantity) item(s),
    @Model.ComputeTotalValue().ToString("c");
    </span>
    @Html.ActionLink("Checkout", "Index", "Cart", new { returnurl = Request.Url.PathAndQuery }, null);
</div>